git ini -- unica vez por cada repositorio
git status --- verifica si esta subido
git add .  ------ apunto de agregar
git remote add origin https://github.com/Osqui2015/rollschol.git -- indica el origen a donde copiar unica vez
git commit -m "first commit" ---- verifica un comentario anterior
git push -u origin master ----
git branch -M main	----	


actualizar:

git


